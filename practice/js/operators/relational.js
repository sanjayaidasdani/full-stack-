// in js relational operators are used its operands and determine
// the relationship between them . they return a boolean value 
//(true and false ) based on the comparison result 

const obj = {length: 10};
console.log("length" in obj); // true
console.log([]instanceof Array); // true


// it check if a property exists in an object 
// instanceof check if an object is an instance of a constructor 
